import 'package:flutter/material.dart';

/// Aba Perfil: avatar com iniciais, nível e XP, indicadores, conquistas,
/// botão Sobre e botão Sair. Conteúdo real construído na fase do chat
/// da EdIA, perfil e sobre.
class PerfilScreen extends StatelessWidget {
  const PerfilScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Perfil — em construção'));
  }
}
