import 'package:flutter/material.dart';

/// Aba Cursos: lista vertical dos cursos, com busca, filtro por
/// categoria e contador de resultados. Conteúdo real construído na fase
/// de dashboard e listagem de cursos.
class CursosScreen extends StatelessWidget {
  const CursosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Cursos — em construção'));
  }
}
