import 'package:flutter/material.dart';

/// Aba Salas: lista das Salas de Estudo, com indicação de ao vivo,
/// ocupação e lotação. Conteúdo real construído na fase de detalhe de
/// curso e salas de estudo.
class SalasScreen extends StatelessWidget {
  const SalasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Salas de Estudo — em construção'));
  }
}
