import 'package:flutter/material.dart';

/// Aba Início: saudação, indicadores, alerta preditivo em destaque,
/// "Continue de onde parou", salas de hoje e o bloco de Modo áudio.
/// Conteúdo real construído na fase de dashboard e listagem de cursos.
class InicioScreen extends StatelessWidget {
  const InicioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Início — em construção'));
  }
}
